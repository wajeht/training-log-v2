<% if (user.is_client == true) { %>
  <i class="bi bi-check-circle-fill text-primary" style="font-size: 1rem"></i>
<% } %>
<% if (user.is_admin == true) { %>
  <i class="bi bi-lightning-fill text-primary" style="font-size: 1rem"></i>
<% } %>
